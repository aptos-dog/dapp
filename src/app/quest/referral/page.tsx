export default function Referral({ className = "" }) {
  return (
    <div className={`bg-white shadow rounded-lg p-4 ${className}`}>
      <h2 className="font-bold mb-2">Referral</h2>
      <p>Coming soon...</p>
    </div>
  );
}
